/**
 * Dependencies.
 */

var gobs = require('gobs')


module.exports = function(state) {
  var store = {}
  return {
    add: function(before, topic, transition, after) {

    },

    emit: function(topic) {

    }
  }
}
